<h2>メモ一覧</h2>

<% if @memos.empty? %>
  <p>メモがありません</p>
<% else %>
  <ul>
    <% @memos.each do |memo| %>
      <li>
        <a href="/memos/<%= memo.id %>">
          <%= h(memo.title) %>
        </a>
      </li>
    <% end %>
  </ul>
<% end %>
